#include <bits/stdc++.h>

typedef long long ll;

#define fio(in, out) freopen(in, "r", stdin); freopen(out, "w", stdout);
#define cio() ios::sync_with_stdio(0); cin.tie(0);
#define endl '\n'

const int mod = 1e9+7;
template<typename T>
void amax(T& a, T b) { a=a>b?a:b; }
template<typename T>
void amin(T& a, T b) { a=a<b?a:b; }

using namespace std;

typedef struct node {
    char x;
    bool isend;
    map<char, node*> children;
    node(char key) : x(key) { isend = false; }
} node;

//none to original - nog
map<string, string> nog;
set<string> has; // has word wiht no vowels

int main() {
    cio();
    cin >> n;
    for(int i=0; i

    return 0;
}
