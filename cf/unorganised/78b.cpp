#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    string s = "ROYGBIV";

    int n;
    cin >> n;
    for(int i=0; i<n/7; ++i)
        cout << s;
    int tmp = n%7;
    cout < ,
    cout << endl;

    return 0;
}
